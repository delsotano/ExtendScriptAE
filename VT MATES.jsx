// Creación de mates según prompt y ancho de la compoapp.beginUndoGroup("VT Mates");var comp = app.project.activeItem;if ((comp == null) || (!(comp instanceof CompItem))){    alert("Selecciona la compo");    }var num = prompt("Cúantas mates quieres", undefined, "2");var ancho = Math.ceil(comp.width/num); // Utilizo un ceil para hacer una variable Integeralert(ancho);var miArray = []; // no lo defino porque no se cúanto va a valer num;for (i = 0; i < num; i++){    var color = 1/(i+1);    miArray[i]= comp.layers.addSolid([color,color,color],"Mate_"+i.toString(),ancho,comp.height,comp.pixelAspect,comp.duration);    if (i>0) {        miArray[i].moveAfter(miArray[i-1])        }    miArray[i].property("Transform").property("Anchor Point").setValue([0,comp.height/2]);    miArray[i].property("Transform").property("Position").setValue([i*ancho,comp.height/2]);    }app.endUndoGroup();